public class Forbidden extends Polygon{ //Vi skapar en Forbidden typ som ärver egenskaper och metoder från Polygon

    public Forbidden(Edge[] edges, int[] xarray, int[] yarray, int npoint, int id) {
        super(edges, xarray, yarray, npoint, id);
    }
}